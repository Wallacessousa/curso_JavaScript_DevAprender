// OPERADOR TERNÁRIO

// Tem um cliente, 100 pontos = comum. 200 =premium.
// Exemplo 1:
let pontos = 99;
let tipo = pontos > 100 ? 'premium' : 'comum';  // Maior que 100 = premium, se não, comum.
console.log(tipo);

// Isso tira a necessidade de ficar usando IF ELIF ELSE toda hora.